import styled from 'styled-components';
import { flexcenter } from 'styles';

// eslint-disable-next-line import/prefer-default-export
export const CreatoHeader = styled.header`
  ${flexcenter}
  box-shadow: inset 0 -1px 0 0 #323234;
  height: 64px;
`;
